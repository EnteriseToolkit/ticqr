<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools">

	<item
		android:id="@+id/action_send_order"
		android:icon="@drawable/ic_action_send"
		android:showAsAction="ifRoom|withText"
		android:title="@string/menu_send_order"
		app:showAsAction="ifRoom|withText"
		tools:ignore="AppCompatResource,UnusedAttribute"/>

	<item
		android:id="@+id/action_rescan"
		android:showAsAction="never"
		android:title="@string/menu_rescan"
		app:showAsAction="never"
		tools:ignore="AppCompatResource,UnusedAttribute"/>

</menu>
